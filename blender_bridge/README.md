# Blender Bridge (stub)
Export landmarks JSON and use Blender scripts to overlay.

Example:
blender --background --python blender_bridge/scripts/setup_scene.py -- --video outputs/throw.mp4 --landmarks outputs/throw_landmarks.json --out outputs/throw_blender_overlay.mp4